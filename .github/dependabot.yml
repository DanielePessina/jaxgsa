version: 2
updates:
  # Keep the GitHub Actions used by CI / publish workflows up to date.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns: ["*"]
