# Analyze (Sobol)

Variance-based Sobol indices (`S1`, `ST`, optional `S2`) computed from model
outputs evaluated on a Saltelli design from `gsax.sobol.sample()`.

The canonical API reference now lives at [API Reference](/api/).

Jump directly to:

- [`gsax.sobol.analyze()`](/api/#sobol)
- [`gsax.sobol.SobolResult`](/api/#sobol)

Related docs:

- [Bootstrap Confidence Intervals](/examples/bootstrap)
- [xarray Output](/examples/xarray)
- [Methods](/guide/methods)
